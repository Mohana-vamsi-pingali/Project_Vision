DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5434/project_vision

# GCP Configuration
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
GCS_BUCKET=your-bucket-name

# Worker Trigger Configuration
# Modes: local (subprocess), cloudrun (Google Cloud Run Jobs API)
JOB_RUNNER_MODE=local
WORKER_PATH=worker.py
CLOUD_RUN_JOB_NAME=vision-worker
